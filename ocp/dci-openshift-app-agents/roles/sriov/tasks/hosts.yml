---
  
all:
  vars:
    du_ptp_int: ens3f0
    du_sriov_int: ens1f0
    flexran_du_ns: flexran-test-ns
  children:
    jumphosts: ## Only one jumphost
      hosts:
        jumphost:
          ansible_connection: local
    baremetal_workers:
      hosts:
        worker1:
          hostname: worker1
          ansible_user: core
          namespace: default

